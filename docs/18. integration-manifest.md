∩╗┐# Dual Workflow Integration Baseline Manifest (Stage 0)

╨ö╨░╤é╨░ ╤ä╨╕╨║╤ü╨░╤å╨╕╨╕: 2026-08-20
╨ÿ╨╜╤é╨╡╨│╤Ç╨░╤å╨╕╨╛╨╜╨╜╨░╤Å ╨▓╨╡╤é╨║╨░: `codex/dual-workflow-stage1`

---

## 1. ╨í╤Ç╨╡╨┤╨░ ╨▓╤ï╨┐╨╛╨╗╨╜╨╡╨╜╨╕╤Å

* **Node.js**: `v20.18.0`
* **npm**: `10.8.2`
* **Python**: `3.14.0`

---

## 2. ╨ÿ╨╖╨╛╨╗╤Å╤å╨╕╤Å ╨╕ ╨┐╤â╤é╨╕ ╤ç╨╕╤ü╤é╤ï╤à Integration-Worktree

╨ÿ╨╜╤é╨╡╨│╤Ç╨░╤å╨╕╨╛╨╜╨╜╨░╤Å ╤Ç╨░╨╖╤Ç╨░╨▒╨╛╤é╨║╨░ ╨╕╨╖╨╛╨╗╨╕╤Ç╨╛╨▓╨░╨╜╨░ ╨▓ ╤ç╨╕╤ü╤é╤ï╤à worktree ╨╛╤é ╨╖╨░╤ä╨╕╨║╤ü╨╕╤Ç╨╛╨▓╨░╨╜╨╜╤ï╤à ╨▒╨░╨╖╨╛╨▓╤ï╤à ╨║╨╛╨╝╨╝╨╕╤é╨╛╨▓:

| ╨á╨╡╨┐╨╛╨╖╨╕╤é╨╛╤Ç╨╕╨╣ | ╨º╨╕╤ü╤é╤ï╨╣ Integration Worktree | ╨æ╨░╨╖╨╛╨▓╤ï╨╣ Commit | ╨Æ╨╡╤é╨║╨░ |
| :--- | :--- | :--- | :--- |
| **CartonBuilder** | `C:\Projects\CartonBuilder_1-stage1` | `941cca45` | `codex/dual-workflow-stage1` |
| **Packaging Box Designer** | `C:\Projects\Packaging Box Designer-stage1` | `fecbb422` | `codex/dual-workflow-stage1` |
| **CartonFoldViewer** | `C:\Projects\CartonFoldViewer-stage1` | `8b96f57` | `codex/dual-workflow-stage1` |

### ╨í╨╛╤ü╤é╨╛╤Å╨╜╨╕╨╡ ╨╕╤ü╤à╨╛╨┤╨╜╤ï╤à ╨║╨░╤é╨░╨╗╨╛╨│╨╛╨▓ ╨╕ ╨┐╤Ç╨░╨▓╨╕╨╗╨╛ ┬½╨╜╨╡ staging┬╗:
* ╨ÿ╤ü╤à╨╛╨┤╨╜╤ï╨╣ ╨║╨░╤é╨░╨╗╨╛╨│ `C:\Projects\CartonBuilder_1` ╨╛╤ü╤é╨░╤æ╤é╤ü╤Å ╨╜╨░ ╨▓╨╡╤é╨║╨╡ `master` ╤ü ╨╗╨╛╨║╨░╨╗╤î╨╜╤ï╨╝╨╕ HDRI/UI-╨╕╨╖╨╝╨╡╨╜╨╡╨╜╨╕╤Å╨╝╨╕;
* ╨ÿ╤ü╤à╨╛╨┤╨╜╤ï╨╣ ╨║╨░╤é╨░╨╗╨╛╨│ `C:\Projects\Packaging Box Designer` ╨╛╤ü╤é╨░╤æ╤é╤ü╤Å ╨╜╨░ ╨▓╨╡╤é╨║╨╡ `main` ╤ü ╨╜╨╡╤é╤Ç╨╛╨╜╤â╤é╤ï╨╝ `tmp/`;
* ╨ÿ╤ü╤à╨╛╨┤╨╜╤ï╨╣ ╨║╨░╤é╨░╨╗╨╛╨│ `C:\Projects\CartonFoldViewer` ╨╛╤ü╤é╨░╤æ╤é╤ü╤Å ╨╜╨░ ╨▓╨╡╤é╨║╨╡ `master` ╤ü ╨┤╨╕╨░╨│╨╜╨╛╤ü╤é╨╕╤ç╨╡╤ü╨║╨╕╨╝╨╕ ╤ü╨║╤Ç╨╕╨┐╤é╨░╨╝╨╕ `tests/*.py`;
* **╨ƒ╤Ç╨░╨▓╨╕╨╗╨╛ ┬½╨╜╨╡ staging┬╗**: ╨ƒ╨╛╨╗╤î╨╖╨╛╨▓╨░╤é╨╡╨╗╤î╤ü╨║╨╕╨╡ ╨╕ ╨▓╤Ç╨╡╨╝╨╡╨╜╨╜╤ï╨╡ ╤ä╨░╨╣╨╗╤ï ╨╕╨╖ ╨╕╤ü╤à╨╛╨┤╨╜╤ï╤à ╨║╨░╤é╨░╨╗╨╛╨│╨╛╨▓ ╨╜╨╡ ╨┤╨╛╨▒╨░╨▓╨╗╤Å╤Ä╤é╤ü╤Å ╨▓ stage ╨╕ ╨╜╨╡ ╨║╨╛╨╝╨╝╨╕╤é╤Å╤é╤ü╤Å. ╨Æ╤ü╤Å ╨╕╨╜╤é╨╡╨│╤Ç╨░╤å╨╕╤Å ╨▓╨╡╨┤╤æ╤é╤ü╤Å ╤ü╤é╤Ç╨╛╨│╨╛ ╨▓ worktree `*-stage1`.

---

## 3. ╨ù╨░╤ä╨╕╨║╤ü╨╕╤Ç╨╛╨▓╨░╨╜╨╜╤ï╨╡ ╤ì╤é╨░╨╗╨╛╨╜╨╜╤ï╨╡ ╨░╤Ç╤é╨╡╤ä╨░╨║╤é╤ï

* **Packaging Box Designer `v1.2.0`**:
  * ╨á╨░╨╖╨╝╨╡╤Ç: `583,642` ╨▒╨░╨╣╤é╨░
  * SHA-256: `0c5560eff56147dc17fc268639cd346c59bdea6d35d510cc2d581e0c9fd5c1fc`
  * ╨Ü╨╛╨╜╤é╤Ç╨░╨║╤é╤ï: `pbd.model.v1`, `pbd.svg.v4`
* **CartonFoldViewer `v2.3.0`** (Reference baseline):
  * ╨á╨░╨╖╨╝╨╡╤Ç: `741,215` ╨▒╨░╨╣╤é
  * SHA-256: `7b85e361535802b89fdd55ba73fac69c2358b8bb6970edfe9d6f9a16298f27ca`

---

## 4. ╨ó╨╛╤ç╨╜╤ï╨╡ ╨║╨╛╨╝╨░╨╜╨┤╤ï ╨╕ ╤Ç╨╡╨╖╤â╨╗╤î╤é╨░╤é╤ï ╨▓╨╡╤Ç╨╕╤ä╨╕╨║╨░╤å╨╕╨╕ Baseline (╨¡╤é╨░╨┐ 0)

### 4.1. CartonBuilder (`CartonBuilder_1-stage1`)
* **╨Ü╨╛╨╝╨░╨╜╨┤╤ï**:
  ```bash
  npm run test:unit
  npm run build
  npm run test:e2e:smoke
  git diff --check
  ```
* **╨á╨╡╨╖╤â╨╗╤î╤é╨░╤é╤ï**:
  * Unit tests: **64/64 ╤ä╨░╨╣╨╗╨╛╨▓, 342/342 ╤é╨╡╤ü╤é╨░ ╨ƒ╨á╨₧╨¿╨¢╨ÿ** (`PASS`)
  * Production build: **Vite build ╤â╤ü╨┐╨╡╤ê╨╜╨╛ ╤ü╤ä╨╛╤Ç╨╝╨╕╤Ç╨╛╨▓╨░╨╜ ╨▓ `dist/`** (`PASS`)
  * Smoke E2E: **4/4 ╤é╨╡╤ü╤é╨░ ╨ƒ╨á╨₧╨¿╨¢╨ÿ** (`PASS`)
  * `git diff --check`: **0 ╨╛╤ê╨╕╨▒╨╛╨║, tracked tree ╤ç╨╕╤ü╤é╨╛╨╡** (`PASS`)

### 4.2. Packaging Box Designer (`Packaging Box Designer-stage1`)
* **╨Ü╨╛╨╝╨░╨╜╨┤╤ï** (╨╕╨╖ `PROJECT_STATUS.md` line 457, ╨▒╨╡╨╖ ╨┤╨╡╤ü╤é╤Ç╤â╨║╤é╨╕╨▓╨╜╨╛╨│╨╛ `verify:m6:ste`):
  ```bash
  git status --short
  git log -3 --oneline --decorate
  node scripts/current-regression.mjs
  node scripts/m2-evidence-verify.mjs
  node scripts/m4-generated-engine-dual-run.mjs
  git diff --check
  ```
* **╨á╨╡╨╖╤â╨╗╤î╤é╨░╤é╤ï**:
  * Current regression: **PASS** (╨▓╨║╨╗╤Ä╤ç╨░╤Å Dust, Tuck Root, RTE Golden Matrix, M4/M5/M6 ╨║╨╛╨╜╤é╤Ç╨░╨║╤é╤ï)
  * M2 evidence verification: **PASS** (52 referenced artifacts)
  * M4 generated engine dual-run: **PASS** (121 assertions)
  * `git diff --check`: **0 ╨╛╤ê╨╕╨▒╨╛╨║, tracked tree ╤ç╨╕╤ü╤é╨╛╨╡** (`PASS`)

### 4.3. CartonFoldViewer (`CartonFoldViewer-stage1`)
* **╨Ü╨╛╨╝╨░╨╜╨┤╤ï**:
  ```bash
  python tests/run_regression.py
  git diff --check
  ```
* **╨á╨╡╨╖╤â╨╗╤î╤é╨░╤é╤ï**:
  * Master Test & Regression Suite: **9/9 ╤ü╤î╤Ä╤é╨╛╨▓ ╨ƒ╨á╨₧╨¿╨¢╨ÿ** (`PASS`)
  * `git diff --check`: **0 ╨╛╤ê╨╕╨▒╨╛╨║, tracked tree ╤ç╨╕╤ü╤é╨╛╨╡** (`PASS`)

---

## 5. ╨ª╨╡╨╗╨╡╨▓╤ï╨╡ ╤é╤Ç╨╡╨▒╨╛╨▓╨░╨╜╨╕╤Å ╨╕╨╜╤é╨╡╨│╤Ç╨░╤å╨╕╨╕ (Target Architecture)

1. **╨Ü╨╛╨╜╤é╤Ç╨░╨║╤é ╨╛╨▒╨╝╨╡╨╜╨░**:
   * ╨Æ╨╡╤Ç╤ü╨╕╨╛╨╜╨╕╤Ç╨╛╨▓╨░╨╜╨╜╤ï╨╣ bundle: `carton-workflow.v1`
   * ╨Ü╨░╨╜╨╛╨╜╨╕╤ç╨╡╤ü╨║╨░╤Å ╤ü╨╡╨╝╨░╨╜╤é╨╕╤ç╨╡╤ü╨║╨░╤Å ╨╝╨╛╨┤╨╡╨╗╤î: `pbd.model.v1`
   * ╨ƒ╤Ç╨╛╨╕╨╖╨▓╨╛╨┤╨╜╨░╤Å SVG-╤Ç╨░╨╖╨▓╤æ╤Ç╤é╨║╨░: `pbd.svg.v4`
2. **╨ÿ╨╖╨╛╨╗╤Å╤å╨╕╤Å ╨╕ ╤ü╨▒╨╛╤Ç╨║╨░ (Release 2 / Release 3)**:
   * **Three.js ╨╗╨╛╨║╨░╨╗╤î╨╜╨╛, ╨▓╨╡╤Ç╤ü╨╕╤Å 0.185.1 ╨▒╨╡╨╖ CDN** ΓÇö ╨╛╨▒╤Å╨╖╨░╤é╨╡╨╗╤î╨╜╨╛╨╡ ╤å╨╡╨╗╨╡╨▓╨╛╨╡ ╤é╤Ç╨╡╨▒╨╛╨▓╨░╨╜╨╕╨╡ ╨┤╨╗╤Å ╨▒╤â╨┤╤â╤ë╨╡╨│╨╛ Viewer `2.4.0` (╤é╨╡╨║╤â╤ë╨╕╨╣ Viewer `2.3.0` ╨╕╤ü╨┐╨╛╨╗╤î╨╖╤â╨╡╤é Three.js `0.180.0` ╤ç╨╡╤Ç╨╡╨╖ CDN ╨║╨░╨║ reference baseline).
   * ╨¥╨╕╨║╨░╨║╨╕╤à ╤ü╨╡╤é╨╡╨▓╤ï╤à ╨╖╨░╨┐╤Ç╨╛╤ü╨╛╨▓ ╨╕╨╗╨╕ ╨╛╨▒╤Ç╨░╤ë╨╡╨╜╨╕╨╣ ╨║ `C:\Projects\...` ╨╕╨╖ CI/build.
   * ╨ö╨╡╤é╨╡╤Ç╨╝╨╕╨╜╨╕╤Ç╨╛╨▓╨░╨╜╨╜╨░╤Å ╨║╨╛╨╝╨░╨╜╨┤╨░ ╤ü╨╕╨╜╤à╤Ç╨╛╨╜╨╕╨╖╨░╤å╨╕╨╕ ╨│╨╛╤é╨╛╨▓╤ï╤à ╨░╤Ç╤é╨╡╤ä╨░╨║╤é╨╛╨▓ ╨▓ `vendor/plugins/` ╤ü ╨┐╤Ç╨╛╨▓╨╡╤Ç╨║╨╛╨╣ SHA-256 ╨╕ ╨▓╨╡╤Ç╤ü╨╕╨╣.
3. **╨æ╨╡╨╖╨╛╨┐╨░╤ü╨╜╨╛╤ü╤é╤î host-╨┐╤Ç╨╛╤é╨╛╨║╨╛╨╗╨░**:
   * ╨Æ╨░╨╗╨╕╨┤╨░╤å╨╕╤Å `origin`, `sessionId`, `contractVersion`, ╤Ç╨░╨╖╨╝╨╡╤Ç╨░ payload ╨╕ `sha256`.

---

## 6. ╨ƒ╨╗╨░╨╜ ╨¡╤é╨░╨┐╨░ 1A (╨▓╤ï╨┐╨╛╨╗╨╜╨╡╨╜)

1. ╨ù╨░╤ä╨╕╨║╤ü╨╕╤Ç╨╛╨▓╨░╤é╤î TypeScript/JS ╤ü╤à╨╡╨╝╤â `carton-workflow.v1`.
2. ╨á╨╡╨░╨╗╨╕╨╖╨╛╨▓╨░╤é╤î ╤ç╨╕╤ü╤é╤ï╨╡ ╨▓╨░╨╗╨╕╨┤╨░╤é╨╛╤Ç╤ï:
   * envelope validator;
   * `pbd.model.v1` validator;
   * `pbd.svg.v4` metadata validator;
   * SHA-256/integrity helpers.
3. ╨ù╨░╤ä╨╕╨║╤ü╨╕╤Ç╨╛╨▓╨░╤é╤î golden fixtures ╨┤╨╗╤Å ╤é╤Ç╤æ╤à ╤ü╨╡╨╝╨╡╨╣╤ü╤é╨▓ (RTE, STE, TT_SL123/A55).
4. ╨ƒ╤Ç╨╛╨▓╨╡╤Ç╨╕╤é╤î ╨║╨╛╨╜╤é╤Ç╨░╨║╤é ╨╕ ╨▓╨░╨╗╨╕╨┤╨░╤é╨╛╤Ç╤ï ╤é╨╡╤ü╤é╨░╨╝╨╕ ╨┐╨╡╤Ç╨╡╨┤ ╨┐╨╡╤Ç╨╡╤à╨╛╨┤╨╛╨╝ ╨║ Host UI ╨╕ ╤ü╨▒╨╛╤Ç╨║╨╡ ╨┐╨╗╨░╨│╨╕╨╜╨░.

---

## 7. ╨ñ╨╕╨║╤ü╨░╤å╨╕╤Å ╤Ç╨╡╨╖╤â╨╗╤î╤é╨░╤é╨╛╨▓ ╨¡╤é╨░╨┐╨░ 1A (Stage 1A Completion)

╨ö╨░╤é╨░ ╨╖╨░╨║╤Ç╤ï╤é╨╕╤Å: 2026-08-20

* **╨ù╨░╤ä╨╕╨║╤ü╨╕╤Ç╨╛╨▓╨░╨╜╨╜╤ï╨╡ ╨║╨╛╨╝╨╝╨╕╤é╤ï**:
  * **Packaging Box Designer**: `538fb7c` (╨▓╨║╨╗╤Ä╤ç╨░╤Å ╨┐╤Ç╨╡╨┤╤ê╨╡╤ü╤é╨▓╤â╤Ä╤ë╨╕╨╣ `09e0b14`)
  * **CartonBuilder**: `2567791`
* **╨Ü╨╛╨╜╤é╤Ç╨░╨║╤é**: `carton-workflow.v1`
* **Package Manifest SHA-256**: `CF295B35F36E82CFB79B0BF41DD977A2ED8F65A51610523619471383C8BF6BD7` (21 ╨▓╤à╨╛╨┤╨╜╨╛╨╣ ╤ä╨░╨╣╨╗, LF endings, byte-for-byte ╤ü╨╛╨▓╨┐╨░╨┤╨╡╨╜╨╕╨╡ ╤ü HEAD)
* **XML Parser**: `@xmldom/xmldom: 0.9.11` (╤ü ╨╝╨╕╨│╤Ç╨░╤å╨╕╨╡╨╣ ╨╜╨░ API `onError`)
* **╨á╨╡╨╖╤â╨╗╤î╤é╨░╤é╤ï ╨┐╤Ç╨╛╨▓╨╡╤Ç╨╛╨║**:
  * PBD contract verification: **119/119 checks PASS**
  * PBD M2 evidence verification: **PASS**
  * PBD M4 generated dual-run: **121 assertions PASS**
  * CartonBuilder unit tests: **65/65 ╤ä╨░╨╣╨╗╨╛╨▓, 373/373 ╤é╨╡╤ü╤é╨░ PASS**
  * CartonBuilder production build: **Vite build SUCCESS**
  * CartonBuilder smoke E2E: **4/4 ╤ü╤å╨╡╨╜╨░╤Ç╨╕╨╡╨▓ Playwright PASS**
  * Fresh-worktree reproducibility: **PASS (100% bit-for-bit ╤ü╨╛╨▓╨┐╨░╨┤╨╡╨╜╨╕╨╡ ╤à╨╡╤ê╨╡╨╣ ╨╝╨░╨╜╨╕╤ä╨╡╤ü╤é╨░)**
  * ╨£╨░╨╜╨╕╤ä╨╡╤ü╤é╤ï ╨▓ ╨╛╨▒╨╛╨╕╤à ╤Ç╨╡╨┐╨╛╨╖╨╕╤é╨╛╤Ç╨╕╤Å╤à: **╨╕╨┤╨╡╨╜╤é╨╕╤ç╨╜╤ï**
  * ╨í╨╛╤ü╤é╨╛╤Å╨╜╨╕╨╡ worktrees: **╤ç╨╕╤ü╤é╨╛╨╡, `git diff --check` ╨▒╨╡╨╖ ╨╛╤ê╨╕╨▒╨╛╨║**

---

## 8. ╨ñ╨╕╨║╤ü╨░╤å╨╕╤Å ╨╕╤ü╨┐╤Ç╨░╨▓╨╗╨╡╨╜╨╜╨╛╨│╨╛ ╨¡╤é╨░╨┐╨░ 1B (Plugin Packaging and Vendoring)

╨ö╨░╤é╨░ ╨▓╨╡╤Ç╨╕╤ä╨╕╨║╨░╤å╨╕╨╕: 2026-08-21

### 8.1. ╨Ü╨╛╨╜╤é╤Ç╨░╨║╤é╤ï ╨╝╨░╨╜╨╕╤ä╨╡╤ü╤é╨╛╨▓

* `plugin.manifest.v1` ╨╛╨┐╨╕╤ü╤ï╨▓╨░╨╡╤é ╨╛╨┤╨╕╨╜ ╤ü╨░╨╝╨╛╤ü╤é╨╛╤Å╤é╨╡╨╗╤î╨╜╤ï╨╣ plugin package ╨╕ ╨╜╨╡
  ╤Å╨▓╨╗╤Å╨╡╤é╤ü╤Å ╨║╨░╤é╨░╨╗╨╛╨│╨╛╨╝.
* `plugins.manifest.v1` ╨╛╨┐╨╕╤ü╤ï╨▓╨░╨╡╤é host-╨║╨░╤é╨░╨╗╨╛╨│. ╨Ü╨░╨╢╨┤╨░╤Å ╨╖╨░╨┐╨╕╤ü╤î ╤ü╨╛╨┤╨╡╤Ç╨╢╨╕╤é
  ╨║╨░╨╜╨╛╨╜╨╕╤ç╨╡╤ü╨║╨╕╨╣ `manifestPath`, byte length/SHA-256 ╤ü╨░╨╝╨╛╤ü╤é╨╛╤Å╤é╨╡╨╗╤î╨╜╨╛╨│╨╛ ╨╝╨░╨╜╨╕╤ä╨╡╤ü╤é╨░ ╨╕
  ╨┐╨╛╨╗╨╜╤â╤Ä ╤ü╨╡╨╝╨░╨╜╤é╨╕╤ç╨╡╤ü╨║╤â╤Ä ╨║╨╛╨┐╨╕╤Ä plugin descriptor.
* Host ╨▓╨░╨╗╨╕╨┤╨╕╤Ç╤â╨╡╤é ╨╛╨▒╨╡ schema ╨┤╨╛ ╨▓╤ï╤ç╨╕╤ü╨╗╨╡╨╜╨╕╤Å destination ╨╕ ╨┤╨╛ ╨╗╤Ä╨▒╤ï╤à ╨╕╨╖╨╝╨╡╨╜╨╡╨╜╨╕╨╣
  ╤ä╨░╨╣╨╗╨╛╨▓╨╛╨╣ ╤ü╨╕╤ü╤é╨╡╨╝╤ï. `id` ╨╕ `version` ╨┤╨╛╨┐╨╛╨╗╨╜╨╕╤é╨╡╨╗╤î╨╜╨╛ ╨┐╤Ç╨╛╨▓╨╡╤Ç╤Å╤Ä╤é╤ü╤Å ╨║╨░╨║ ╨╛╨┤╨╕╨╜╨╛╤ç╨╜╤ï╨╡
  ╨▒╨╡╨╖╨╛╨┐╨░╤ü╨╜╤ï╨╡ ╤ü╨╡╨│╨╝╨╡╨╜╤é╤ï, ╨░ ╨╕╤é╨╛╨│╨╛╨▓╤ï╨╣ ╨┐╤â╤é╤î ╨╛╨▒╤Å╨╖╨░╨╜ ╨╛╤ü╤é╨░╨▓╨░╤é╤î╤ü╤Å ╨▓╨╜╤â╤é╤Ç╨╕
  `vendor/plugins`.

### 8.2. Producer artifact ╨╕ provenance

* PBD source commit: `6293cc98d743369275a42049fbc359a0d9a8bb6d`.
* Plugin entrypoint: `583,947` ╨▒╨░╨╣╤é,
  SHA-256 `6087faed708b4e3f8edd3ff0eb67fce74a2cd0c9ae5856e19166748b2d5dc78e`.
* Standalone manifest SHA-256:
  `3e4452053576ed80edfebc4ae3b8947fce1ba2eec9217d8cba507b23e2eae154`.
* ╨₧╤ä╨╕╤å╨╕╨░╨╗╤î╨╜╤ï╨╣ PBD plugin builder ╨╖╨░╨┐╤â╤ü╨║╨░╨╡╤é╤ü╤Å ╤é╨╛╨╗╤î╨║╨╛ ╨┐╤Ç╨╕ ╤ü╨╛╨▓╨┐╨░╨┤╨╡╨╜╨╕╨╕ tracked
  inputs ╤ü `HEAD`; ╤ü╨│╨╡╨╜╨╡╤Ç╨╕╤Ç╨╛╨▓╨░╨╜╨╜╤ï╨╣ release HTML ╨┤╨╛╨┐╨╛╨╗╨╜╨╕╤é╨╡╨╗╤î╨╜╨╛ ╤ü╨▓╨╡╤Ç╤Å╨╡╤é╤ü╤Å ╤ü
  committed artifact byte-for-byte.
* Plugin HTML ╨┐╨╛╨╗╤â╤ç╨░╨╡╤é ╤Ç╨╡╨░╨╗╤î╨╜╨╛ ╨┐╤Ç╨╕╨╝╨╡╨╜╤Å╨╡╨╝╤â╤Ä CSP, ╨░ sandbox policy ╨╜╨╡ ╨╕╤ü╨┐╨╛╨╗╤î╨╖╤â╨╡╤é
  ╨╛╨┐╨░╤ü╨╜╤â╤Ä same-origin ╨║╨╛╨╝╨▒╨╕╨╜╨░╤å╨╕╤Ä: ╤é╨╛╨╗╤î╨║╨╛ `allow-scripts`.

### 8.3. Transactional vendoring и build integrity

* `plugins:sync:pbd` полностью выполняет preflight plugin package, embedded
  workflow package и будущего каталога до мутаций.
* Plugin directory, `src/workflow` и `plugins.manifest.json` активируются одной
  multi-target transaction. Ошибка любого rename восстанавливает все ранее
  заменённые назначения.
* `plugins:verify` сверяет самостоятельный manifest с каталогом, его собственный
  SHA-256, все plugin files, entrypoint/artifact, embedded contract, CSP,
  отсутствие лишних файлов и статических внешних URL.
* Vite использует `vendor` как `publicDir`, поэтому проверенные плагины входят в
  публикуемый `dist/plugins`; production build не обращается к соседним
  `C:\Projects\...`.
* Статический scanner не объявляется доказательством runtime offline сам по
  себе. Playwright отдельно загружает опубликованный PBD и требует ноль внешних
  запросов.

### 8.4. Проверки

* PBD plugin contract: **12/12 assertions PASS**.
* PBD M2 evidence: **PASS**; M4 generated dual-run: **121 PASS**.

---

## 9. Фиксация Этапа 1C (Viewer Localization and Standalone Plugin Packaging)

Дата верификации: 2026-08-21

### 9.1. Локализация Three.js и вендоринг

* Three.js 0.185.1 и необходимые аддоны (`three.core.js`, `three.module.js`, `GLTFLoader`, `OrbitControls`, `GLTFExporter`, `BufferGeometryUtils`, `SkeletonUtils`) полностью завендорены локально в `assets/vendor/three/`.
* В `index.html` importmap использует исключительно локальные относительные пути `./assets/vendor/three/`.
* Полностью устранены внешние runtime-зависимости от jsDelivr и других CDN.
* Все версии в `package.json`, `index.html`, `src/config.js` и документации выровнены на `2.4.0` / `v2.4`.

### 9.2. Headless Fold Runtime

* Выделен автономный пакет `@cartonbuilder/fold-runtime@2.4.0` в `runtime/` (`src/runtime/`):
  * `parseSemanticCartonSvg(svgText)`
  * `buildFoldableFromSemanticSvg(svgText, options)`
  * `createHeadlessFoldRuntime({ svgText, name })`
  * `setFoldProgress(value)`
  * `dispose()`
* Пакет снабжён собственным манифестом `package-manifest.json` и `package.json` (чистый ESM export без CJS require).

### 9.3. Producer Artifact & Provenance

* Viewer source commit: `934bd1cc50436a47b6c9317ae5ee696b2edc99f7`.
* Standalone HTML entrypoint: `2,697` байт,
  SHA-256 `663df6339abd1790bc6d9ebd882c19f9418a30472bfe119b05cfee2cc42cdea0`.
* Standalone manifest SHA-256:
  `9aa457cd7a655fafb6e071c8a1be9eaec51fc39532ea2befca5dae01a9c50947`.
* Общий размер пакета плагина: 39 файлов (включая standalone viewer, локальные Three.js модули, стили, ассеты и headless runtime).
* `capabilities.technicalRender` зафиксирован как `false`.
* Standalone builder `scripts/build-viewer-plugin-package.mjs` проверяет чистоту tracked inputs против git HEAD, запрещает symlinks и выполняет валидацию по `plugin.manifest.v1.schema.json`.

### 9.4. Transactional Vendoring в CartonBuilder

* Добавлена команда `npm run plugins:sync:viewer -- --source <path>` (`scripts/sync-viewer-plugin.mjs`).
* Синхронизация использует транзакционную замену через `atomicManifestSync` и обновляет host-каталог `vendor/plugins/plugins.manifest.json` с валидацией `plugins.manifest.v1`.
* Integrity gate `npm run plugins:verify` расширен для валидации обоих плагинов: `packaging-box-designer@1.2.0` и `carton-fold-viewer@2.4.0`.

### 9.5. Результаты верификации

* CartonFoldViewer Master Regression Suite: **9/9 сьютов PASS** (`python tests/run_regression.py`):
  * Module Graph & Importer Structure: PASS
  * Module Import & THREE.js Linter: PASS
  * PBD Semantic SVG Contract (RTE): PASS
  * PBD Semantic SVG Contract (STE): PASS
  * PBD Semantic SVG Contract (TT_SL123): PASS
  * Finite Crease Ribbon Geometry: PASS
  * CAD Bend Deduction Compensation: PASS
  * Snap-Lock 3-Layer Physical Clearance: PASS
  * Authoritative Collision Regression: PASS
* CartonBuilder unit tests: **66/66 файлов, 388/388 тестов PASS** (`npm run test:unit`) — восстановлены все 12 защитных тестов Этапа 1B + добавлены тесты валидации Viewer.
* CartonBuilder build: **PASS** (`prebuild: plugins:verify` + `vite build`).
* CartonBuilder smoke E2E: **7/7 тестов PASS** (`npm run test:e2e:smoke`), включая проверку CSP, отсутствие сетевых запросов, мониторинг отсутствия ошибок `pageerror`, инициализацию 3D WebGL viewport и полное преобразование семантического SVG дилайна в анимированную 3D модель в vendored Viewer.

---

## 10. Stage 2 implementation snapshot (CartonDocument and archive v5)

Verification date: 2026-08-21

The CartonBuilder integration branch now contains the domain and persistence
foundation for the dual workflow:

* `CartonDocument`, `QuickCartonDocument`, `TechnicalCartonDocument` and the
  centralized `createCartonDocument` factory are available under `src/carton/`.
* Technical documents validate the complete `carton-workflow.v1` bundle before
  construction, preserve exact LINE/ARC geometry, include semantic OPEN_CUT
  features, and remain protected from external mutation.
* Project schema v17 stores a discriminated `cartonSource` and independent
  `workflowSelection`. Legacy schemas are
  migrated to `mode: "quick"` without retaining a serialized second `box`
  source of truth.
* `.carton` archive v5 stores technical model and SVG bytes in
  `technical/model.json` and `technical/dieline.svg`, with a dedicated
  `technical/manifest.json`. Technical assets are hash-checked before restore.
* Technical RTE, STE and TT_SL123/A55 fixtures pass byte-preserving archive
  round-trips and tamper rejection tests.

Current verification on the working branch:

* CartonBuilder unit tests: **427/427 PASS** (69 files).
* CartonBuilder production build and vendored plugin gate: **PASS**.
* CartonBuilder smoke E2E: **7/7 PASS**.
* Graphify refreshed: 3,739 nodes and 6,766 edges.

At the time of this snapshot, the Step 1 workflow selector, PBD host iframe
and technical artwork/3D/render consumers remained scheduled for subsequent
workflow integration stages; the first selector/host slice is recorded below.

## 11. Stage 3 workflow selection and PBD host slice

The first Stage 3 implementation slice is now present in the working tree:

* Step 1 exposes two explicit modes: `quick` (Legacy Custom Net) and
  `technical` (Packaging Box Designer). The technical card states that the
  current profiles are reference-only and not production-certified.
* Technical mode loads the vendored PBD as a sandboxed iframe and keeps the
  CartonBuilder stepper visible. There is no separate `Ready` button; the
  normal Step 2 transition is the single guarded hand-off.
* The parent-side `carton-host.v1` protocol validates `event.source`, origin,
  session ID, protocol version and payload size. The guarded transition asks
  PBD for one bundle, then revalidates it through `TechnicalCartonDocument`.
* The PBD release source now emits `plugin:ready`, validation state and a
  hashed `pbd:carton-ready` bundle built from the same current model used for
  its JSON and canonical SVG exports.
* The existing artwork viewport receives a technical compatibility facade from
  the validated `TechnicalCartonDocument`. Technical Preview/Render remain
  disabled until their dedicated technical backends are implemented; this
  prevents a quick-only geometry renderer from being presented as technical.

The CartonBuilder quick workflow remains regression-compatible. The PBD source
restore protocol is now committed in the clean integration worktree and its
official plugin package has been rebuilt and synced transactionally into
`vendor/plugins/packaging-box-designer/1.2.0/`.

The parent host bridge lives in `src/host/pbdHostProtocol.js`, deliberately
outside `src/workflow/`. The latter is a replaceable contract package and is
atomically replaced by `plugins:sync:pbd`; keeping the bridge outside that
destination prevents a plugin sync from deleting application runtime code.

Infrastructure verification for this local slice:

* `npm run plugins:sync:pbd -- --source <local PBD dist package>`: PASS.
* `npm run plugins:verify`: **2/2 plugins PASS**.
* `npm run build`: PASS.
* Host protocol unit tests: **6/6 PASS**; the three large archive/rollback
  scenarios also pass with a 20-second Vitest timeout (their default 5-second
  threshold is too low on this Windows workspace).
* The dev server is available at `http://127.0.0.1:5173/` and serves the
  vendored PBD entrypoint with the `carton-host.v1` marker.

## 12. Stage 3 restore, checkpoint and provenance acceptance

Verification date: 2026-08-21

* `workflowSelection` is project state, not a second carton source. A
  technical draft may persist with `workflowSelection: "technical"` while the
  committed `cartonSource` remains the last quick source. Local storage is
  read only as the default for a new session; opening an archive or autosave
  always wins.
* `ProjectCheckpointStore` provides transactional
  `createProjectCheckpoint()`, `restoreProjectCheckpoint()` and
  `discardProjectCheckpoint()` APIs. The checkpoint contains the snapshot,
  artwork blobs, render assets and technical model/SVG assets. Verify/write
  failures leave the previous checkpoint untouched.
* Before technical model replacement, technical-editor re-entry or workflow
  switch, CartonBuilder creates and validates a checkpoint before mutating the
  live document. A cancelled or failed transition therefore keeps the active
  project unchanged, while ordinary autosave remains separate.
* Official PBD provenance: source commit `67444de2a0fcf65857974ad6ba35f9b699d69e59`
  is the clean branch HEAD and is recorded by the vendored manifest. The
  official plugin artifact `index.html` is `598034` bytes with SHA-256
  `d798b23784c800c625d9ad42e31878f824567f9df43c47f60fb6fa5c2d9c3715`; its
  `plugin-manifest.json` SHA-256 is
  `b3fd595590566b6933950280cba7323e551ac9e04076cf9d42a3b99193e24ae9`.
  The temporary detached `Packaging Box Designer-fix-build` worktree and its
  `d08c2a2` provenance are removed.
* The official PBD plugin build passed from the real branch, and the vendored
  plugin gate passed after synchronization. The producer's historical
  `current-regression.mjs` reference-only branch/name and external fixture
  gates remain reported separately; they are not silently claimed as PASS.
* Final local verification: CartonBuilder unit tests **444/444** (72 files),
  `npm run plugins:verify` **2/2**, production build PASS, the required
  technical restore E2E **3/3**, workflow-selection E2E **1/1**, and smoke E2E
  **7/7**.
* The known non-product `current-regression.mjs` failures are exactly:
  `BASELINE_BRANCH_MAIN` on `codex/dual-workflow-stage1`, four historical
  `M6 STE KutuModeli Parameterized SVG Action Boundary` source-hash checks,
  `M6 STE Physical Validation Access Boundary` Smilepack snapshot bytes/hash,
  and `M6 STE Smilepack PrintQ A2420 Boundary` page-snapshot hash. All other
  current-regression gates in that run passed.
